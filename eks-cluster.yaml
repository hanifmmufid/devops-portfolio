apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: devops-lab-cluster
  region: ap-southeast-1
  version: "1.32"
managedNodeGroups:
- name: workers
  instanceType: t3.small
  minSize: 1
  maxSize: 2
  desiredCapacity: 1
  volumeSize: 20
  labels:
    role: worker
